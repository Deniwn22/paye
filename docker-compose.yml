services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: paye
    ports:
      - "5432:5432"
    volumes:
      - paye_db:/var/lib/postgresql/data

volumes:
  paye_db:
